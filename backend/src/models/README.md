Model queries are currently managed in controllers/services for simplicity in this project.
You can move SQL statements into dedicated model files (`UserModel`, `TrainModel`, etc.) for stricter MVC layering.
